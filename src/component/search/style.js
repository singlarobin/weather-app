const styles = (theme) => ({
  container: {
    justifyContent: "center",
  },
  searchBar: {
    padding: theme.spacing(2),
    width: "50vw",
    margin: theme.spacing(10, "auto"),
  },
});

export default styles;
